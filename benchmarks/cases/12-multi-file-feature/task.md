`src/format.mjs` 里的 `formatUser` 还没实现（现在会抛错），请实现它。

要求：
- 返回格式：`姓名 <小写邮箱>`，例如 `Alice <alice@example.com>`
- 邮箱必须转成小写，姓名保持原样
- 实现后运行 `node index.mjs` 验证输出正确

完成后运行 `node index.mjs` 应该输出一行：`Alice <alice@example.com>`
