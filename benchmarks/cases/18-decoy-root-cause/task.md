`node --test` 有测试失败，报错都出现在金额格式化上：`'1.5' !== '1.50'`、`'1' !== '1.00'` 之类的小数位对不上。

请修复，让所有测试通过。

数据格式：每行 `<name>,<amount>`，`amount` 是金额。

规范与隐藏约束（必须全部满足；验证会独立于 `test/` 检查，可见测试没有覆盖的部分也算数）：

- 只允许修改 `src/` 下的代码，**不得修改 `test/`**（验证会逐字节比对测试文件）。
- 空字段按 0 处理：`parseRow("apple,")` 返回对象的 `amount` 必须是数字 `0`。
- **非法字段必须抛错**：`parseRow("apple,abc")` 必须抛异常。可见测试**没有**覆盖这一条，但不允许把非法值静默当成 0 吞掉。
- 负数、含空字段的混合数据、上千行的大数组都必须正确求和与格式化。
- 三个模块的公开导出名与参数个数不得改变：
  - `src/parse.mjs`：`parseRow(line)`、`parseRows(text)`；`amount` 必须是数字。
  - `src/sum.mjs`：`sumRows(rows)`，返回数字。
  - `src/format.mjs`：`formatAmount(amount)`，返回保留两位小数的字符串。

修完运行 `node --test` 确认全绿。
